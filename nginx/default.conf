server {
    listen 80;
    server_name _;

    location / {
        return 200 "ZTD Server Frontend Gateway\n";
        add_header Content-Type text/plain;
    }

    location /api/ {
        return 200 "ZTD API Gateway - Backend not connected yet\n";
        add_header Content-Type text/plain;
    }
}
